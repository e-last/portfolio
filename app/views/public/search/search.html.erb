<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <div class="float-right">
        <%= render "public/relationships/search_area" %>
      </div>
      <% if @users.exists? %>
        <%= render 'public/relationships/followings', users: @users %>
      <% else %>
        <h2>フォローしているユーザーはいません</h2>
      <% end %>
    </div>
  </div>
</div>

<%= render 'public/relationships/followings', users: @users %>
